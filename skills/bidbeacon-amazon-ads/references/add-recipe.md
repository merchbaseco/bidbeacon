# Recipe: add a missing workflow

Use only when the user explicitly asks to add or revise a recipe in this skill.

1. Preserve the original request as an evaluation case.
2. Confirm no existing recipe handles it without awkward branching.
3. Add the smallest durable direction that changes agent behavior; leave schemas and discoverable facts to the MCP.
4. Update the router only when the new branch needs distinct invocation.
5. Validate, package, and forward-test the original request.

## Done

Report what the new recipe teaches, why existing guidance was insufficient, and the evaluation result.
